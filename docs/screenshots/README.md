# Screenshots

The README links to `demo.gif` in this folder. Record roughly fifteen seconds:
sign in, open a product, place an order, then run the concurrency demo with
locking off so the stock goes negative on screen. That last part is the reason
anyone watches.

Worth capturing as stills alongside it:

- `dashboard.png` — the revenue chart over nine months of seeded data
- `products.png` — the catalogue with search and filters
- `order-detail.png` — an order with its stock effect
- `concurrency-demo.png` — the results panel after a run

Sign in as `asha@stockledger.test` / `password123` for all of them.
