{
  "name": "stockledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-tenant inventory and order management for small retailers.",
  "scripts": {
    "dev": "concurrently -n server,frontend -c cyan,magenta \"npm --prefix server run dev\" \"npm --prefix frontend run dev\"",
    "setup": "npm install && npm --prefix server install && npm --prefix frontend install",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm --prefix server run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "eslint-plugin-react-refresh": "^0.4.18",
    "globals": "^15.14.0",
    "prettier": "^3.4.2"
  }
}
